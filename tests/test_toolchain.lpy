(ns tests.test-toolchain
  "Phase 0 smoke test: balli.core resolves from the test runner."
  (:require [basilisp.test :refer [deftest is]]
            [balli.core :as b]))

(deftest smoke
  (is (= "0.2.0" b/version)))
